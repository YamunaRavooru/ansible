- name: variables from files
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print the course information
    ansible.builtin.dabug:
     msg: "course is {{ Course }}, Duration  is {{ Duration }}, Trainer is {{ Trainer }}"